path: /RnD/dog_yolo/datasets  # 데이터셋의 루트 디렉토리
train: train/images  # 학습 이미지 경로 (상대 경로)
val: val/images  # 검증 이미지 경로 (상대 경로)

# 클래스 정보
nc: 1  # 클래스 수를 지정하세요
names: ['dog']  # 클래스 이름을 지정하세요

# 데이터셋 타입
task: pose

# flip_idx: [0,4,1,5,2,6,3,7,8,12,9,13,10,14,11,15] # flip each pair number based on sequence, if not leave it
# Keypoint 정보 추가
kpt_shape: [17, 3]
